{
  "private": true,
  "scripts": {
    "docs": "pulp docs",
    "bower": "bower install",
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "bower install && pulp build --no-check-main --modules Test -- --stash --censor-codes=WildcardInferredType,ImplicitImport,ImplicitQualifiedImport && pulp test",
    "repl": "pulp repl",
    "build": "pulp browserify --to ../templates/play/ps.julius"
  },
  "devDependencies": {
    "bower": "^1.8.4",
    "purescript": "^0.12.0",
    "pulp": "^12.3.0",
    "purescript-psa": "^0.7.3",
    "rimraf": "^2.6.2",
    "uglify-js": "^3.4.9",
    "uuid": "^3.3.2",
    "uuid-validate": "^0.0.3"
  },
  "dependencies": {}
}
