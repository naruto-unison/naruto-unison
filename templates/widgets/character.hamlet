<div .charbox>
  <div>
    <img data-name=#{ident} .char.head src=/img/ninja/#{ident}/icon.jpg>
  <div>
    ^{Link.character char}
    <ul>
      $forall skillGroup <- skills
        <li>
          $forall Skill{name} <- toList skillGroup
            <span .skill data-name=#{ident}>#{name}
